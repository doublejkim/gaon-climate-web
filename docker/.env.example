# docker compose 빌드 시 주입되는 변수입니다.
# 이 파일을 복사해 docker/.env 로 만든 뒤 값을 채우세요.
#   cp docker/.env.example docker/.env
#
# 주의: VITE_API_BASE_URL 은 빌드 시점에 정적 파일로 인라인됩니다.
#       값을 바꾸면 반드시 `docker compose up -d --build` 로 재빌드해야 합니다.

# 배포 환경의 API 서버 주소
VITE_API_BASE_URL=https://api.example.com
